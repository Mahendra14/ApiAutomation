---
swagger: "2.0"
info:
  description: "APIs to cater for Back Office Operations functionality in OPERA Cloud.\
    \ <br /><br /> A common BackOffice industry term is End of Day.\_ This closes\
    \ and balances each day's business activities. It reconciles guest folios and\
    \ processes credit card transactions. The End of Day routine rolls the business\
    \ date forward and prints final reports."
  version: "21.1.1"
  title: "Early Release OPERA Cloud Back Office Operations API"
  termsOfService: "https://www.oracle.com/legal/terms.html"
  contact:
    email: "hospitality_apis_ww_grp@oracle.com"
  license:
    name: "UPL"
    url: "https://opensource.org/licenses/upl"
basePath: "/bof/v0"
tags:
- name: "BackOfficeOperations"
  description: "This module provides APIs to service Back Office operations for property,\
    \ such as managing end-of-day, getting the properties current business date, and\
    \ perform cashier closure."
schemes:
- "https"
produces:
- "application/json"
paths:
  /hotels/{hotelId}/businessDate:
    get:
      tags:
      - "BackOfficeOperations"
      summary: "Fetch end of day business dates"
      description: "Use this API to find out the current business date of a specified\
        \ hotel. <p><strong>OperationId:</strong>getBusinessDate</p>"
      operationId: "getBusinessDate"
      parameters:
      - name: "hotelId"
        in: "path"
        description: "Unique ID that identifies a single hotel property."
        required: true
        type: "string"
        maxLength: 2000
        minLength: 1
      - name: "authorization"
        in: "header"
        description: "Bearer token that needs to be passed which is generated post\
          \ user authentication"
        required: true
        type: "string"
      - name: "x-app-key"
        in: "header"
        description: "Client or Partner’s Application Key"
        required: true
        type: "string"
        pattern: "^[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89aAbB][a-f0-9]{3}-[a-f0-9]{12}$"
      - name: "x-hotelid"
        in: "header"
        description: "Mandatory parameter to identify the hotel code where the end\
          \ user is logged in"
        required: true
        type: "string"
      - name: "x-externalsystem"
        in: "header"
        description: "External system code."
        required: false
        type: "string"
        maxLength: 80
        x-example: "EXTERNALSYSTEMCODE"
      - name: "Accept-Language"
        in: "header"
        description: "Language code"
        required: false
        type: "string"
      responses:
        "200":
          description: "Response for fetching active end of day status for multiple\
            \ hotels."
          examples:
            application/json:
              hotels:
              - hotelId: "HOTEL1"
                businessDate: "2020-04-05"
          headers:
            Content-Language:
              type: "string"
              description: "Audience language"
              pattern: "[a-zA-Z]{1,8}(-[a-zA-Z0-9]{1,8})*"
              x-example: "en-GB"
          schema:
            $ref: "#/definitions/businessDate"
        "204":
          description: "BusinessDate not found."
definitions:
  businessDate:
    type: "object"
    properties:
      hotels:
        description: "Collection of hotels and its active end of days."
        $ref: "#/definitions/hotelActiveEndOfDaysType"
      links:
        $ref: "#/definitions/links"
      warnings:
        $ref: "#/definitions/warningsType"
    description: "Response for fetching active end of day status for multiple hotels."
  hotelActiveEndOfDaysType:
    type: "array"
    description: "Collection of hotels and its active end of days."
    items:
      $ref: "#/definitions/hotelActiveEndOfDayType"
    maxItems: 4000
  hotelActiveEndOfDayType:
    type: "object"
    properties:
      hotelId:
        type: "string"
        description: "Hotel code of the active end of days."
        minLength: 0
        maxLength: 20
      businessDate:
        type: "string"
        format: "date"
        description: "Current open business date of hotel."
    description: "Defines hotel and its active end of days."
  links:
    type: "array"
    items:
      $ref: "#/definitions/instanceLink"
  instanceLink:
    type: "object"
    required:
    - "href"
    - "method"
    - "operationId"
    - "rel"
    properties:
      href:
        type: "string"
        description: "URI [RFC3986] or URI Template [RFC6570]. If the value is set\
          \ to URI Template, then the \"templated\" property must be set to true."
      rel:
        type: "string"
        description: "Name of the link relation that, in addition to the type property,\
          \ can be used to retrieve link details. For example, href or profile."
      templated:
        type: "boolean"
        description: "Boolean flag that specifies that \"href\" property is a URI\
          \ or URI Template. If the property is a URI template, set this value to\
          \ true. By default, this value is false."
        default: false
      method:
        type: "string"
        description: "HTTP method for requesting the target of the link."
        enum:
        - "GET"
        - "POST"
        - "PUT"
        - "DELETE"
        - "PATCH"
        - "OPTIONS"
        - "HEAD"
      targetSchema:
        type: "string"
        description: "Link to the metadata of the resource, such as JSON-schema, that\
          \ describes the resource expected when dereferencing the target resource.."
      operationId:
        type: "string"
        description: "The operationId of the path you can call to follow this link.\
          \  This allows you to look up not only the path and method, but the description\
          \ of that path and any parameters you need to supply."
      title:
        type: "string"
        description: "Exact copy of the \"summary\" field on the linked operation."
    description: "Metadata describing link description objects that MAY appear in\
      \ the JSON instance representation."
  warningsType:
    type: "array"
    description: "Used in conjunction with the Success element to define a business\
      \ error."
    items:
      $ref: "#/definitions/warningType"
    maxItems: 4000
  warningType:
    type: "object"
    properties:
      value:
        type: "string"
        description: "Property Value"
      shortText:
        type: "string"
        description: "An abbreviated version of the error in textual format."
        maxLength: 2000
      code:
        type: "string"
        description: "If present, this refers to a table of coded values exchanged\
          \ between applications to identify errors or warnings."
        minLength: 0
        maxLength: 20
      docURL:
        type: "string"
        description: "If present, this URL refers to an online description of the\
          \ error that occurred."
        maxLength: 2000
      status:
        type: "string"
        description: "If present, recommended values are those enumerated in the ErrorRS,\
          \ (NotProcessed Incomplete Complete Unknown) however, the data type is designated\
          \ as string data, recognizing that trading partners may identify additional\
          \ status conditions not included in the enumeration."
        maxLength: 2000
      tag:
        type: "string"
        description: "If present, this attribute may identify an unknown or misspelled\
          \ tag that caused an error in processing. It is recommended that the Tag\
          \ attribute use XPath notation to identify the location of a tag in the\
          \ event that more than one tag of the same name is present in the document.\
          \ Alternatively, the tag name alone can be used to identify missing data\
          \ [Type=ReqFieldMissing]."
        maxLength: 2000
      recordId:
        type: "string"
        description: "If present, this attribute allows for batch processing and the\
          \ identification of the record that failed amongst a group of records. This\
          \ value may contain a concatenation of a unique failed transaction ID with\
          \ specific record(s) associated with that transaction."
        maxLength: 2000
      type:
        type: "string"
        description: "The Warning element MUST contain the Type attribute that uses\
          \ a recommended set of values to indicate the warning type. The validating\
          \ XSD can expect to accept values that it has NOT been explicitly coded\
          \ for and process them by using Type =\"Unknown\"."
        minLength: 0
        maxLength: 20
      language:
        type: "string"
        description: "Language identification."
        pattern: "[a-zA-Z]{1,8}(-[a-zA-Z0-9]{1,8})*"
      rph:
        type: "string"
        description: "Reference Place Holder used as an index for this warning."
        minLength: 1
        maxLength: 8
    description: "Used when a message has been successfully processed to report any\
      \ warnings or business errors that occurred."
  exceptionDetailType:
    type: "object"
    required:
    - "title"
    - "type"
    properties:
      type:
        type: "string"
        description: "Absolute URI [RFC3986] that identifies the problem type.  When\
          \ dereferenced, it SHOULD provide a human-readable summary of the problem\
          \ (for example, using HTML)."
      title:
        type: "string"
        description: "Short, human-readable summary of the problem.  The summary SHOULD\
          \ NOT change for subsequent occurrences of the problem, except for purposes\
          \ of localization."
      status:
        type: "integer"
        description: "HTTP status code for this occurrence of the problem, set by\
          \ the origin server."
      detail:
        type: "string"
        description: "Human-readable description specific to this occurrence of the\
          \ problem."
      instance:
        type: "string"
        description: "Absolute URI that identifies the specific occurrence of the\
          \ problem.  It may or may not provide additional information if dereferenced."
      o:errorCode:
        type: "string"
        description: "Application error code, which is different from HTTP error code."
      o:errorPath:
        type: "string"
        description: "Path to the problem at the resource or property level."
      o:errorDetails:
        type: "array"
        description: "Details of the error message, consisting of a hierarchical tree\
          \ structure."
        items:
          $ref: "#/definitions/errorInstance"
      links:
        $ref: "#/definitions/links"
    title: "Error Detail"
    description: "Complex type that contains error details for a REST call."
  errorInstance:
    type: "object"
    required:
    - "title"
    - "type"
    properties:
      type:
        type: "string"
        description: "Absolute URI [RFC3986] that identifies the problem type.  When\
          \ dereferenced, it SHOULD provide a human-readable summary of the problem\
          \ (for example, using HTML)."
      title:
        type: "string"
        description: "Short, human-readable summary of the problem.  The summary SHOULD\
          \ NOT change for subsequent occurrences of the problem, except for purposes\
          \ of localization."
      status:
        type: "integer"
        description: "HTTP status code for this occurrence of the problem, set by\
          \ the origin server."
      detail:
        type: "string"
        description: "Human-readable description specific to this occurrence of the\
          \ problem."
      instance:
        type: "string"
        description: "Absolute URI that identifies the specific occurrence of the\
          \ problem.  It may or may not provide additional information if dereferenced."
      o:errorCode:
        type: "string"
        description: "Application error code, which is different from HTTP error code."
      o:errorPath:
        type: "string"
        description: "Path to the problem at the resource or property level."
    title: "Error Instance Details"
    description: "Complex type that contains error instance details for a REST call."
parameters:
  authKey:
    name: "authorization"
    in: "header"
    description: "Bearer token that needs to be passed which is generated post user\
      \ authentication"
    required: true
    type: "string"
  x-app-key:
    name: "x-app-key"
    in: "header"
    description: "Client or Partner’s Application Key"
    required: true
    type: "string"
    pattern: "^[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89aAbB][a-f0-9]{3}-[a-f0-9]{12}$"
  x-hotelid:
    name: "x-hotelid"
    in: "header"
    description: "Mandatory parameter to identify the hotel code where the end user\
      \ is logged in"
    required: true
    type: "string"
  x-externalsystem:
    name: "x-externalsystem"
    in: "header"
    description: "External system code."
    required: false
    type: "string"
    maxLength: 80
    x-example: "EXTERNALSYSTEMCODE"
  Accept-Language:
    name: "Accept-Language"
    in: "header"
    description: "Language code"
    required: false
    type: "string"
responses:
  "400":
    description: "Bad Request"
    schema:
      $ref: "#/definitions/exceptionDetailType"
  "401":
    description: "Unauthorized"
  "403":
    description: "Forbidden"
  "404":
    description: "Resource not found"
    schema:
      $ref: "#/definitions/exceptionDetailType"
  "405":
    description: "Method not allowed"
  "406":
    description: "Not acceptable."
  "413":
    description: "Request Entity Too Large"
  "414":
    description: "Request URI Too Large"
  "415":
    description: "Unsupported Media Type"
  "500":
    description: "System Error"
    schema:
      $ref: "#/definitions/exceptionDetailType"
  "502":
    description: "Bad Gateway"
  "503":
    description: "Service Unavailable"
externalDocs:
  description: "Find out more about Oracle Hospitality"
  url: "https://docs.oracle.com/en/industries/hospitality/integration_platforms.html"
